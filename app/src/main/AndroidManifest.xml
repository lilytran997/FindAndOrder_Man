<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.npmt.findandorder">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/find_and_order"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/find_and_order_round"
        android:supportsRtl="true"
        android:theme="@style/Fullscreen2">
        <activity
            android:name=".Intro"
            android:theme="@style/FullscreenMan">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".InsertDirectory"/>
        <activity android:name=".ChooseRestaurant" />
        <activity android:name=".ChooseFood" />
        <activity android:name=".CustomerIfomation" />
        <activity android:name=".GetBill" />
        <activity android:name=".Finish" />
        <activity
            android:name=".AddRest"
            android:windowSoftInputMode="adjustPan">
        </activity>
    </application>

</manifest>